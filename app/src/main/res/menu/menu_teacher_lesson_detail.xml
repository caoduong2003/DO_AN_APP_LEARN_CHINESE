<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Edit Action -->
    <item
        android:id="@+id/action_edit"
        android:icon="@drawable/ic_edit"
        android:title="Chỉnh sửa"
        app:showAsAction="ifRoom" />

    <!-- Share Action -->
    <item
        android:id="@+id/action_share"
        android:icon="@drawable/ic_share"
        android:title="Chia sẻ"
        app:showAsAction="ifRoom" />

    <!-- Refresh Action -->
    <item
        android:id="@+id/action_refresh"
        android:icon="@drawable/ic_refresh"
        android:title="Làm mới"
        app:showAsAction="ifRoom" />

    <!-- Delete Action -->
    <item
        android:id="@+id/action_delete"
        android:icon="@drawable/ic_delete"
        android:title="Xóa"
        app:showAsAction="never" />

    <!-- Toggle Status -->
    <item
        android:id="@+id/action_toggle_status"
        android:title="Thay đổi trạng thái"
        app:showAsAction="never" />

    <!-- Toggle Premium -->
    <item
        android:id="@+id/action_toggle_premium"
        android:title="Thay đổi premium"
        app:showAsAction="never" />

    <!-- Duplicate -->
    <item
        android:id="@+id/action_duplicate"
        android:title="Nhân bản"
        app:showAsAction="never" />

</menu>